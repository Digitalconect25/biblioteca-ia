---
// El antiguo panel basado en localStorage fue reemplazado por el panel con
// cuenta real (Supabase). Mantenemos la URL viva redirigiendo a /dashboard.
export const prerender = false;
return Astro.redirect('/dashboard', 301);
---
